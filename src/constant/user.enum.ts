export enum UserStatus {
    InActive = 1,
    Active = 2,
    Blocked = 4,
}

export enum MemberShip {
    Standard = 1,
    Prime = 2,
}

export enum UserRole {
    SuperAdmin = 1,
    Admin = 2,
    User = 4,
    Narrator = 8,
}
